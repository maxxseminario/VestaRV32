#=======================================================================
# Makefrag for boot code 
# #-----------------------------------------------------------------------

boot_sc = \
    boot \

boot_p_tests = $(addprefix boot-p-, $(boot_sc))
